apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-idle-monitor-config
  namespace: your-namespace
data:
  POSTGRES_URL: "jdbc:postgresql://your_postgres_host:5432/mydatabase?sslmode=require"
  INTERVAL_MINUTES: "10"
  IDLE_THRESHOLD: "1 hour"
